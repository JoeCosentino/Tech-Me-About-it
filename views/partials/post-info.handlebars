<article>
    <div>
        <div>{{title}}<span>{{format_date created_at}}</span></div>
        <div>{{body}}</div>
        <div>{{user.username}}</div>
    </div>
    <div>
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</article>